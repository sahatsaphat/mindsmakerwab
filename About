<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
  <!-- Page Title for About Us -->
  <title>About Us - Mind Maker</title>
  <link rel="icon" href="https://placehold.co/32x32/3A53A4/FFFFFF?text=MM" type="image/png">
  <link rel="preconnect" href="https://fonts.googleapis.com">
  <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
  <link href="https://fonts.googleapis.com/css2?family=Kanit:wght@300;400;500;700&family=Lexend+Deca:wght@400;700&family=Special+Gothic+Expanded+One&display=swap" rel="stylesheet">
  <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/5.15.4/css/all.min.css">
  <style>
    /* --- Base Variables & Styles (from original code) --- */
    :root {
      --primary-blue: #3A53A4;
      --primary-pink: #E6559E;
      --primary-yellow: #F9D423;
      --text-dark: #222;
      --text-light: #fff;
      --bg-light: #fff;
      --bg-dark: #111;
      --bg-medium: #f4f4f4;
      --font-special: "Special Gothic Expanded One", sans-serif;
      --font-body: "Lexend Deca", sans-serif;
      --font-kanit: "Kanit", sans-serif;
    }

    * { margin: 0; padding: 0; box-sizing: border-box; }
    html { scroll-behavior: smooth; }
    body {
      font-family: var(--font-body);
      line-height: 1.6;
      color: var(--text-dark);
      background-color: var(--bg-light);
      overflow-x: hidden;
    }
    a { text-decoration: none; color: inherit; }
    ul { list-style: none; }
    .container { max-width: 1200px; margin: 0 auto; padding: 0 2rem; }
    h1, h2, h3, h4, h5, h6 { font-weight: 700; }
    h1 { font-family: var(--font-special); }
    h2 { font-size: clamp(2rem, 5vw, 3rem); margin-bottom: 2rem; text-align: center; }

    /* --- Navbar (from original code) --- */
    .navbar {
      background-color: transparent;
      padding: 1rem 0;
      position: sticky; top: 0; z-index: 1000;
      transition: all 0.4s ease;
    }
    .navbar-inner {
        display: flex;
        justify-content: space-between;
        align-items: center;
        max-width: 1200px;
        margin: 0 auto;
        padding: 0.5rem 2rem;
        border-radius: 0;
        background-color: transparent;
        transition: all 0.4s cubic-bezier(0.25, 0.46, 0.45, 0.94);
    }
    .navbar.navbar-scrolled .navbar-inner {
        max-width: 900px;
        background-color: rgba(255, 255, 255, 0.85);
        backdrop-filter: blur(10px);
        border-radius: 999px;
        box-shadow: 0 8px 30px rgba(0,0,0,0.1);
        border: 1px solid #eee;
        margin-top: 0.5rem;
    }
    .logo {
      font-size: 1.5rem;
      font-weight: bold;
      font-family: var(--font-special);
      transition: all 0.3s ease;
    }
    .nav-center { display: flex; gap: 1rem; transition: gap 0.4s ease; }
    .nav-center a { font-weight: 500; transition: color 0.3s; padding: 0.5rem 1rem; position: relative;}
    .nav-center a:hover { color: var(--primary-pink); }
    
    .nav-center a.active {
        color: var(--primary-pink);
        font-weight: 700;
    }

    .navbar.navbar-scrolled .nav-center { gap: 0.5rem; }
    .portfolio-link a {
      font-weight: bold; padding: 0.6rem 1.5rem;
      border: 2px solid var(--text-dark); border-radius: 999px;
      transition: all 0.3s;
    }
    .portfolio-link a:hover { background-color: var(--text-dark); color: var(--text-light); }
    .nav-toggle { display: none; }
    
    /* --- Page Header (Marquee - from original code) --- */
    .page-header {
        padding: 0;
        background-color: var(--bg-dark);
        color: var(--text-light);
        overflow: hidden;
    }
    .marquee-wrapper {
        display: flex;
        align-items: center;
        white-space: nowrap;
        padding: 1.5rem 0; 
    }
    .marquee-content {
        display: inline-block;
        animation: marquee 35s linear infinite;
    }
    .marquee-content h1 {
        font-size: clamp(1.5rem, 3.75vw, 1.875rem);
        display: inline-block;
        margin: 0 1.5rem;
        color: var(--text-light);
        font-family: var(--font-special);
        text-transform: uppercase;
        letter-spacing: 2px;
    }
    @keyframes marquee {
        0% { transform: translateX(0); }
        100% { transform: translateX(-100%); }
    }

    /* --- NEW: About Page Specific Styles --- */
    .about-section {
      padding: 6rem 0;
    }

    .about-intro-wrapper {
      max-width: 800px;
      margin: 0 auto;
      text-align: center;
    }
    .about-intro-wrapper h2 {
      min-height: 1.2em; /* Ensure space for typing animation */
      margin-bottom: 2rem;
      font-family: var(--font-body);
      font-weight: 700;
      font-size: clamp(1.8rem, 5vw, 2.5rem);
    }
     .about-intro-wrapper h2 .typing-cursor {
      animation: blink 1s step-end infinite;
    }
    @keyframes blink {
      from, to { opacity: 1; }
      50% { opacity: 0; }
    }
    .about-intro-wrapper p {
      font-size: 1.1rem;
      line-height: 1.8;
      color: #555;
    }
    
    .mission-vision-grid {
      display: grid;
      grid-template-columns: repeat(auto-fit, minmax(300px, 1fr));
      gap: 3rem;
      margin-top: 5rem;
      text-align: left;
    }

    .mission-vision-item {
      padding: 2.5rem;
      border-radius: 20px;
      background-color: var(--bg-medium);
      border: 1px solid #e0e0e0;
    }

    .mission-vision-item .icon {
      font-size: 2.5rem;
      color: var(--primary-pink);
      margin-bottom: 1.5rem;
    }

    .mission-vision-item h3 {
      font-size: 1.5rem;
      margin-bottom: 1rem;
      font-family: var(--font-body);
    }
    /* --- Achievements Section Styles --- */
.achievements-section {
  background-color: #111; /* var(--bg-dark) */
  color: #fff; /* var(--text-light) */
  padding: 6rem 2rem;
  
  
}

.achievements-section h2 {
    color: #fff; /* var(--text-light) */
    text-align: center;
    margin-bottom: 3rem;
    font-family: "Special Gothic Expanded One", sans-serif; /* var(--font-special) */
    font-size: clamp(2.5rem, 5vw, 3.5rem);
}

.achievements-grid {
  display: grid;
  grid-template-columns: repeat(auto-fit, minmax(250px, 1fr));
  gap: 3rem;
  text-align: center;
}

.achievement-item .counter {
  font-family: "Special Gothic Expanded One", sans-serif; /* var(--font-special) */
  font-size: clamp(3.5rem, 8vw, 5rem);
  line-height: 1;
  color: #E6559E; /* var(--primary-pink) */
  margin-bottom: 0.5rem;
  display: inline-block;
}

.achievement-item h4 {
  font-family: "Lexend Deca", sans-serif; /* var(--font-body) */
  font-size: 1.1rem;
  margin-top: 0.5rem;
  font-weight: 400;
  color: #ccc;
}

/* --- Animation on Scroll --- */
.fade-in {
    opacity: 0;
    transform: translateY(30px);
    transition: opacity 0.8s ease-out, transform 0.8s ease-out;
}

.fade-in.is-visible {
    opacity: 1;
    transform: translateY(0);
}

    .values-section {
        background-color: var(--bg-dark);
        color: var(--text-light);
        padding: 6rem 0;
        margin-top: 5rem;
    }
    .values-section h2 {
        color: var(--text-light);
    }
    .values-grid {
        display: grid;
        grid-template-columns: repeat(auto-fit, minmax(250px, 1fr));
        gap: 2.5rem;
        margin-top: 3rem;
    }
    .value-item {
        text-align: center;
        padding: 2rem;
    }
    .value-item .icon {
        font-size: 3rem;
        color: var(--primary-pink);
        margin-bottom: 1.5rem;
        transition: transform 0.3s ease;
    }
    .value-item:hover .icon {
        transform: scale(1.1) rotate(-5deg);
    }
    .value-item h4 {
        font-size: 1.2rem;
        margin-bottom: 0.5rem;
    }
     .value-item p {
        font-size: 0.95rem;
        color: #ccc;
        line-height: 1.7;
    }

    .cta-section {
        padding: 6rem 0;
    }

    .cta-box {
      background-color: var(--primary-pink);
      color: var(--text-light);
      padding: 4rem;
      border-radius: 20px;
      text-align: center;
    }
    .cta-box h2 {
        text-align: center;
        color: var(--text-light);
        font-family: var(--font-body);
        font-size: clamp(1.8rem, 4vw, 2.5rem);
    }
    .cta-box p {
        max-width: 600px;
        margin: 0 auto 2.5rem auto;
        font-size: 1.1rem;
    }
    .cta-button {
      font-family: var(--font-body);
      background-color: var(--bg-dark);
      color: var(--primary-pink);
      padding: 1rem 3rem;
      border: none;
      border-radius: 50px;
      font-size: 1rem;
      font-weight: 500;
      cursor: pointer;
      transition: all 0.3s ease;
      text-transform: uppercase;
      letter-spacing: 1px;
      display: inline-block;
    }
    .cta-button:hover {
      background-color: #333;
      transform: translateY(-3px);
      box-shadow: 0 4px 15px rgba(0,0,0,0.2);
    }


    /* --- Footer (from original code) --- */
    .footer { background-color: var(--bg-dark); color: var(--text-light); padding: 4rem 2rem 2rem; }
    .footer-container { display: grid; grid-template-columns: 2fr 2fr 2.5fr 1.5fr; gap: 2rem; max-width: 1200px; margin: 0 auto; text-align: left; }
    .footer-column h3 { font-family: var(--font-special); font-size: clamp(1.8rem, 4vw, 2.2rem); margin-bottom: 0.5rem; }
    .footer-column h4 { font-size: 0.85rem; text-transform: uppercase; margin-bottom: 1.2rem; }
    .footer-column p, .footer-column a { font-size: 0.85rem; color: #a0a0a0; transition: color 0.3s; }
    .footer-column a:hover { color: var(--primary-pink); }
    .social-icons { display: flex; gap: 1.2rem; margin-top: 1rem; }
    .social-icons a { font-size: 1.3rem; }
    .footer-bottom { border-top: 1px solid #333; padding-top: 2rem; margin-top: 3rem; color: #777; font-size: 0.9rem; text-align: center; }

    /* --- Responsive & Mobile Menu (from original code) --- */
    @media (max-width: 768px) {
      .navbar-inner { padding: 0.5rem 1rem; }
      .nav-center, .portfolio-link { display: none; }
      .nav-toggle { display: flex; cursor: pointer; padding: 0.5rem; font-size: 1.5rem; z-index: 1001;}
      .footer-container { grid-template-columns: 1fr; text-align: center; }
      .footer-column { margin-bottom: 2rem; }
      .social-icons { justify-content: center;}
    }
    .mobile-overlay-menu {
      position: fixed; top: 0; left: 0; width: 100%; height: 100%;
      background-color: var(--bg-dark); color: var(--text-light);
      z-index: 999; display: flex; flex-direction: column;
      align-items: center; justify-content: center;
      transform: translateX(100%);
      transition: transform 0.4s cubic-bezier(0.77, 0, 0.175, 1);
    }
    .mobile-overlay-menu.active { transform: translateX(0); }
    .mobile-overlay-menu .menu-items { text-align: center; }
    .mobile-overlay-menu .menu-items li { margin: 1.5rem 0; }
    .mobile-overlay-menu .menu-items a { 
        font-size: clamp(1.5rem, 5vw, 2rem); 
        font-family: var(--font-special); 
        transition: color 0.3s ease;
    }
    .mobile-overlay-menu .menu-items a:hover { color: var(--primary-pink); }
    .nav-toggle.is-active { color: var(--text-light); }
    .nav-toggle.is-active i::before { content: '\f00d'; }

    .navbar.mobile-menu-active .logo,
    .navbar.mobile-menu-active .nav-toggle {
        color: var(--text-light);
    }
  </style>
</head>
<body>

  <!-- Navigation Bar (active link updated) -->
  <nav class="navbar" id="navbar">
    <div class="navbar-inner">
      <div class="logo"><a href="index.html">Mind Maker</a></div>
      <ul class="nav-center">
        <!-- The "active" class is now on the About link -->
        <li><a href="About.html" class="active">About</a></li>
        <li><a href="index.html#services">Services</a></li>
        <li><a href="index.html#clients">Clients</a></li>
        <li><a href="Contact.html">Contact</a></li>
      </ul>
      <div class="portfolio-link"><a href="#portfolio">Portfolio</a></div>
      <div class="nav-toggle" id="nav-toggle">
        <i class="fas fa-bars"></i>
      </div>
    </div>
  </nav>

  <!-- Mobile Menu Overlay (structure copied) -->
  <div class="mobile-overlay-menu" id="mobile-overlay-menu">
      <ul class="menu-items">
          <li><a href="About.html">About</a></li>
          <li><a href="index.html#services">Services</a></li>
          <li><a href="index.html#clients">Clients</a></li>
          <li><a href="Contact.html">Contact</a></li>
          <li><a href="#portfolio">Portfolio</a></li>
      </ul>
  </div>

  <main>
    <!-- Page Header Marquee (style reused) -->
    <header class="page-header">
        <div class="marquee-wrapper">
            <div class="marquee-content">
                <h1>OUR STORY・OUR MISSION・OUR VISION・</h1>
                <h1>OUR STORY・OUR MISSION・OUR VISION・</h1>
                <h1>OUR STORY・OUR MISSION・OUR VISION・</h1>
                <h1>OUR STORY・OUR MISSION・OUR VISION・</h1>
            </div>
        </div>
    </header>
    
    <!-- NEW: Main About Section -->
    <section class="about-section">
        <div class="container">

            <!-- Introduction with Typing Animation -->
            <div class="about-intro-wrapper">
                <h2 id="about-heading"></h2>
                <p>
                    Mind Maker was born from a simple yet powerful idea: to transform ordinary events into extraordinary experiences. Founded in the heart of Bangkok, we are a team of creative thinkers, strategists, and production experts dedicated to pushing the boundaries of what's possible. We believe that a successful event is more than just a gathering; it's a story waiting to be told, a memory waiting to be created.
                </p>
            </div>

            <!-- Mission and Vision Grid -->
            <div class="mission-vision-grid">
                <div class="mission-vision-item">
                    <div class="icon"><i class="fas fa-rocket"></i></div>
                    <h3>Our Mission</h3>
                    <p>To design and execute flawless, innovative, and memorable events that connect brands with their audiences in meaningful ways. We strive to be a strategic partner, delivering measurable results and exceeding expectations on every project.</p>
                </div>
                <div class="mission-vision-item">
                    <div class="icon"><i class="fas fa-bullseye"></i></div>
                    <h3>Our Vision</h3>
                    <p>To be Thailand's leading event organizer, recognized for our creative excellence, strategic approach, and unwavering commitment to client success. We aim to set new standards in the industry, one spectacular event at a time.</p>
                </div>
            </div>
        </div>
    </section>
    <section class="section achievements-section">
    <div class="container">
         <h2 class="fade-in">Our Achievements</h2>
        <div class="achievements-grid">
            <div class="achievement-item fade-in">
                <div class="counter" data-target="150">0</div>+
                <h4>Projects Completed</h4>
            </div>
            <div class="achievement-item fade-in">
                <div class="counter" data-target="98">0</div>%
                <h4>Happy Clients</h4>
            </div>
            <div class="achievement-item fade-in">
                <div class="counter" data-target="10">0</div>+
                <h4>Years of Experience</h4>
            </div>
            <div class="achievement-item fade-in">
                <div class="counter" data-target="999">0</div>+
                <h4>Cups of Coffee</h4>
            </div>
        </div>
    </div>
</section>

    <!-- NEW: Core Values Section -->
    <section class="values-section">
        <div class="container">
            <h2>Our Core Values</h2>
            <div class="values-grid">
                <div class="value-item">
                    <div class="icon"><i class="fas fa-lightbulb"></i></div>
                    <h4>Creativity</h4>
                    <p>We infuse imagination and originality into every detail, crafting unique concepts that capture attention and inspire action.</p>
                </div>
                <div class="value-item">
                    <div class="icon"><i class="fas fa-handshake"></i></div>
                    <h4>Partnership</h4>
                    <p>We work collaboratively with our clients, building strong relationships based on trust, transparency, and shared goals.</p>
                </div>
                <div class="value-item">
                    <div class="icon"><i class="fas fa-star"></i></div>
                    <h4>Excellence</h4>
                    <p>We are relentless in our pursuit of perfection, from initial planning to final execution, ensuring quality in everything we do.</p>
                </div>
                 <div class="value-item">
                    <div class="icon"><i class="fas fa-cogs"></i></div>
                    <h4>Innovation</h4>
                    <p>We embrace new technologies and forward-thinking ideas to deliver fresh, modern, and impactful event solutions.</p>
                </div>
            </div>
        </div>
    </section>

    <!-- NEW: Call to Action Section -->
    <section class="cta-section">
        <div class="container">
            <div class="cta-box">
                <h2>Have a Project in Mind?</h2>
                <p>Let's turn your vision into a remarkable reality. We're ready to listen, strategize, and create an experience that your audience will never forget.</p>
                <a href="Contact.html" class="cta-button">Let's Talk</a>
            </div>
        </div>
    </section>

  </main>

  <!-- Footer (copied verbatim) -->
  <footer class="footer">
    <div class="footer-container">
      <div class="footer-column footer-brand"><h3>MIND MAKER</h3><p>Innovative Event Organizer<br>in Thailand</p></div>
      <div class="footer-column footer-contact"><h4>BANGKOK</h4><p><a href="mailto:contact@mindmakerevent.com">contact@mindmakerevent.com</a></p><p><a href="tel:+6621234567">+66 2 123 4567</a></p><p>18 ซอย ประชาราษฎร์บำเพ็ญ 26 แยก 12, สามเสนนอก, ห้วยขวาง, กรุงเทพมหานคร 10310</p><p><a href="https://www.google.com/maps/place/Mind+Maker+Co.,+Ltd.+%E0%B8%9A%E0%B8%A3%E0%B8%B4%E0%B8%A9%E0%B8%B1%E0%B8%97+%E0%B8%A1%E0%B8%B2%E0%B8%A2%E0%B8%94%E0%B9%8C+%E0%B9%80%E0%B8%A1%E0%B8%84%E0%B9%80%E0%B8%81%E0%B8%AD%E0%B8%A3%E0%B9%8C+%E0%B8%88%E0%B8%B3%E0%B8%81%E0%B8%B1%E0%B8%94/@13.793617,100.574948,17z" target="_blank" rel="noopener noreferrer" class="map-link">SEE ON MAP <i class="fas fa-arrow-right" aria-hidden="true"></i></a></p></div>
      <div class="footer-column footer-newsletter"><h4>WANT TO BE THE SMARTEST<br>IN YOUR OFFICE?</h4><p><a href="#" class="newsletter-link">SIGN UP FOR OUR NEWSLETTER <i class="fas fa-arrow-right" aria-hidden="true"></i></a></p></div>
      <div class="footer-column footer-social"><h4>FOLLOW US</h4><div class="social-icons"><a href="https://facebook.com/mindmakerproduction" target="_blank" rel="noopener noreferrer" aria-label="Mind Maker on Facebook" title="Facebook"><i class="fab fa-facebook-f"></i></a><a href="https://instagram.com/mindmaker.official" target="_blank" rel="noopener noreferrer" aria-label="Mind Maker on Instagram" title="Instagram"><i class="fab fa-instagram"></i></a><a href="https://tiktok.com/@mindmaker.official" target="_blank" rel="noopener noreferrer" aria-label="Mind Maker on TikTok" title="TikTok"><i class="fab fa-tiktok"></i></a></div></div>
    </div>
    <div class="footer-bottom">© <span id="currentYear"></span> Mind Maker. All rights reserved.</div>
  </footer>

  <script>
    document.addEventListener('DOMContentLoaded', function() {
        // --- Set current year in footer ---
        document.getElementById('currentYear').textContent = new Date().getFullYear();

        // --- Navbar Scroll Logic (copied) ---
        const navbar = document.getElementById('navbar');
        window.addEventListener('scroll', () => {
            if (!navbar.classList.contains('mobile-menu-active')) {
                navbar.classList.toggle('navbar-scrolled', window.scrollY > 50);
            }
        });
        
        // --- Mobile Menu Logic (copied) ---
        const navToggle = document.getElementById('nav-toggle');
        const mobileMenu = document.getElementById('mobile-overlay-menu');
        if (navToggle && mobileMenu) {
            function toggleMenu(show) {
                const isActive = typeof show === 'boolean' ? show : !mobileMenu.classList.contains('active');
                mobileMenu.classList.toggle('active', isActive);
                navToggle.classList.toggle('is-active', isActive);
                navbar.classList.toggle('mobile-menu-active', isActive); 
                document.body.style.overflow = isActive ? 'hidden' : '';
                
                if (isActive) {
                    navbar.classList.remove('navbar-scrolled');
                } else {
                    if (window.scrollY > 50) {
                        navbar.classList.add('navbar-scrolled');
                    }
                }
            }

            navToggle.addEventListener('click', () => toggleMenu());
            mobileMenu.querySelectorAll('a').forEach(link => {
                link.addEventListener('click', () => toggleMenu(false));
            });
        }

        // --- Heading Typing Animation (adapted for About page) ---
        const aboutHeading = document.getElementById('about-heading');
        if (aboutHeading) {
            const phrases = ["We Create Experiences.", "We Build Brands.", "We Are Mind Maker."];
            aboutHeading.innerHTML = '<span class="typing-text"></span><span class="typing-cursor">|</span>';
            const textSpan = aboutHeading.querySelector('.typing-text');
            const typingSpeed = 100, erasingSpeed = 50, delayBetween = 2000;
            let phraseIndex = 0;
            let charIndex = 0;
            let isDeleting = false;

            function typeLoop() {
                const currentPhrase = phrases[phraseIndex];
                let timeout = isDeleting ? erasingSpeed : typingSpeed;

                if (isDeleting) {
                    textSpan.textContent = currentPhrase.substring(0, charIndex - 1);
                    charIndex--;
                } else {
                    textSpan.textContent = currentPhrase.substring(0, charIndex + 1);
                    charIndex++;
                }

                if (!isDeleting && charIndex === currentPhrase.length) {
                    isDeleting = true;
                    timeout = delayBetween; // Wait before erasing
                } else if (isDeleting && charIndex === 0) {
                    isDeleting = false;
                    phraseIndex = (phraseIndex + 1) % phrases.length; // Move to the next phrase
                    timeout = 500; // Wait before re-typing
                }
                setTimeout(typeLoop, timeout);
            }
            typeLoop();
        }
    });
    document.addEventListener('DOMContentLoaded', function() {

    // --- Intersection Observer for Animations ---
    // สร้าง Observer เพื่อตรวจจับว่า element เข้ามาในหน้าจอหรือยัง
    const observer = new IntersectionObserver((entries) => {
        entries.forEach(entry => {
            // ถ้า element เข้ามาในหน้าจอ
            if (entry.isIntersecting) {
                // เพิ่ม class 'is-visible' เพื่อให้ CSS ทำงาน (fade-in)
                entry.target.classList.add('is-visible');
                
                // หา element ตัวเลข .counter ที่อยู่ในนั้น
                const counter = entry.target.querySelector('.counter');
                
                // ถ้าเจอตัวเลขและยังไม่เคยทำงานแอนิเมชัน
                if (counter && !counter.classList.contains('animated')) {
                    // เริ่มแอนิเมชันตัวเลขนับขึ้น
                    animateCounter(counter);
                    // เพิ่ม class 'animated' เพื่อป้องกันการทำงานซ้ำ
                    counter.classList.add('animated');
                }
            }
        });
    }, { 
        threshold: 0.1 // ให้ทำงานเมื่อ element แสดงในหน้าจอ 10%
    });

    // สั่งให้ Observer เริ่มสังเกตการณ์ทุก element ที่มี class '.achievement-item'
    document.querySelectorAll('.achievement-item').forEach(el => {
        observer.observe(el);
    });
    
    // --- Counter Animation Function ---
    // ฟังก์ชันสำหรับทำให้ตัวเลขนับขึ้น
    function animateCounter(counter) {
        const target = +counter.getAttribute('data-target'); // ดึงค่าเป้าหมายจาก data-target
        const duration = 2000; // ระยะเวลาที่ใช้ในการนับ (2000ms = 2 วินาที)
        let start = 0;

        const step = (timestamp) => {
            if (!start) start = timestamp;
            const progress = Math.min((timestamp - start) / duration, 1);
            counter.textContent = Math.floor(progress * target);
            if (progress < 1) {
                window.requestAnimationFrame(step);
            }
        };
        window.requestAnimationFrame(step);
    }
});
  </script>
</body>
</html>
